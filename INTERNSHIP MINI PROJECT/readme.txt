# Install OpenCV
pip install opencv-python

# Install NumPy
pip install numpy

# Install TensorFlow (includes Keras)
pip install tensorflow
#optional
pip install opencv-contrib-python
 and save the haarcascade_frontalface_default.xml ,emotion_detection_model.h5 and the python code in the same folder 
